<h4> Add a Comment: </h4>

<%= form_for [post, comment] do |f| %>
  <div>
    <%= f.label :body %>
    <%= f.text_area :body, class: 'form-control'%>
    <%= f.submit "Create Comment"%>
    </div>


<% end %>
